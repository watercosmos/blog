spring:
  profiles.active: local

logging:
  config: "src/main/resources/config/log4j2-${spring.profiles.active}.properties"

---

spring:
  profiles: local
  datasource:
      url: "jdbc:mysql://localhost:3306/blog?autoReconnect=true&useUnicode=true&characterEncoding=utf-8&useSSL=false"
      username: root
      password: liuhanyu
      driver-class-name: com.mysql.jdbc.Driver

---

spring:
  profiles: test
  datasource:
      url: "jdbc:mysql://localhost:3306/blog?autoReconnect=true&useUnicode=true&characterEncoding=utf-8&useSSL=false"
      username: root
      password: liuhanyu
      driver-class-name: com.mysql.jdbc.Driver

---

spring:
  profiles: stage
  datasource:
      url: "jdbc:mysql://localhost:3306/blog?autoReconnect=true&useUnicode=true&characterEncoding=utf-8&useSSL=false"
      username: root
      password: liuhanyu
      driver-class-name: com.mysql.jdbc.Driver

---

spring:
  profiles: prod
  datasource:
    url: "jdbc:mysql://localhost:3306/blog?autoReconnect=true&useUnicode=true&characterEncoding=utf-8&useSSL=false"
    username: root
    password: liuhanyu
    driver-class-name: com.mysql.jdbc.Driver
